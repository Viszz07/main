apply plugin: 'java'
/*repositories {
    mavenCentral()    
}

dependencies {
    // No additional dependencies are needed for this example
}
*/
archivesBaseName = 'HelloWorldApp'
jar {
    manifest {
        attributes 'Main-Class': 'HelloWorldApp'
    }
}